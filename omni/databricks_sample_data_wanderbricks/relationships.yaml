- join_from_view: booking_updates
  join_to_view: bookings
  join_type: always_left
  on_sql: ${booking_updates.booking_id} = ${bookings.booking_id}
  relationship_type: many_to_one

- join_from_view: booking_updates
  join_to_view: users
  join_type: always_left
  on_sql: ${booking_updates.user_id} = ${users.user_id}
  relationship_type: many_to_one

- join_from_view: bookings
  join_to_view: users
  join_type: always_left
  on_sql: ${bookings.user_id} = ${users.user_id}
  relationship_type: many_to_one

- join_from_view: bookings
  join_to_view: properties
  join_type: always_left
  on_sql: ${bookings.property_id} = ${properties.property_id}
  relationship_type: many_to_one

- join_from_view: clickstream
  join_to_view: users
  join_type: always_left
  on_sql: ${clickstream.user_id} = ${users.user_id}
  relationship_type: many_to_one

- join_from_view: customer_support_logs
  join_to_view: users
  join_type: always_left
  on_sql: ${customer_support_logs.user_id} = ${users.user_id}
  relationship_type: many_to_one

- join_from_view: employees
  join_to_view: hosts
  join_type: always_left
  on_sql: ${employees.host_id} = ${hosts.host_id}
  relationship_type: many_to_one

- join_from_view: page_views
  join_to_view: users
  join_type: always_left
  on_sql: ${page_views.user_id} = ${users.user_id}
  relationship_type: many_to_one

- join_from_view: page_views
  join_to_view: properties
  join_type: always_left
  on_sql: ${page_views.property_id} = ${properties.property_id}
  relationship_type: many_to_one

- join_from_view: payments
  join_to_view: bookings
  join_type: always_left
  on_sql: ${payments.booking_id} = ${bookings.booking_id}
  relationship_type: many_to_one

- join_from_view: properties
  join_to_view: hosts
  join_type: always_left
  on_sql: ${properties.host_id} = ${hosts.host_id}
  relationship_type: many_to_one

- join_from_view: properties
  join_to_view: destinations
  join_type: always_left
  on_sql: ${properties.destination_id} = ${destinations.destination_id}
  relationship_type: many_to_one

- join_from_view: properties
  join_to_view: property_images
  join_type: always_left
  on_sql: ${properties.property_id} = ${property_images.property_id}
  relationship_type: one_to_one

- join_from_view: reviews
  join_to_view: bookings
  join_type: always_left
  on_sql: ${reviews.booking_id} = ${bookings.booking_id}
  relationship_type: many_to_one

- join_from_view: reviews
  join_to_view: users
  join_type: always_left
  on_sql: ${reviews.user_id} = ${users.user_id}
  relationship_type: many_to_one
